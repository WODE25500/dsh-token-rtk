// dsh-token-rtk
// The bundle value is its one bundled skill. No runtime behavior; exists so the
// bundle registers as a profile layer via its cordis.patch.
export const name = 'token-rtk'
export function apply(ctx, config = {}) {}
